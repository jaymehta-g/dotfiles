These are dotfiles that are symlinked into the home dir using stow.

Under `.config/home-manager` is Nix home-manager configuration. Do not apply home-manager changes, ask the user to apply them instead.

Config for other tools is in `.config/`